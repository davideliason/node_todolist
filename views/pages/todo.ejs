<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-4">
  <title>To Do</title>

  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"> 
</head>

<body class = "container">  
  <header>
     <% include ../partials/header %>
  </header>

  <main class = "jumbotron">

 	  <div class="row">
            <div class="col-sm-4">
            </div>

             <div class="col-sm-4">
                 <% if (tasks.length === 0){ %>
		  			 <h5> No tasks yet </h5>
					<% } else { %>

				<% for (var i=0; i< tasks.length; i++){ %>
				   <ul>
				      <li style="list-style:none, font-size: 2em" ><a  style="color:red" href="/delete/<%= i %>"> x</a> <%= tasks[i] %>  </li>
				   </ul>
					
				<% } } %>
            </div>

            <div class="col-sm-4">
            </div>
      </div>
            
	  <div class="row">	
        <div class="col-sm-2"></div>
        <div class="col-sm-8">

			<form action="/todo/add" method="post">
			add a task: <input type="text" name="task" placeholder="new task.." />
		   		<button style="background-color: blue" type="submit">Confirm</button>
	    	</form> 
        </div>

        <div class="col-sm-2"></div>

	  </div>
    </main>
 <% include ../partials/footer %>
</body>
</html>
